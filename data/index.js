const skills = require('./skill');
const stages = require('./stage');
const cards = require('./card');
const messages = require('./message');
const interpretations = require('./interpretation');
const keywords = require('./keyword');

module.exports = {
  skills,
  stages,
  cards,
  messages,
  interpretations,
  keywords
};
